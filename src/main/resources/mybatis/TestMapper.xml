<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="springboot.TestMapper" >
	<select id="queryRoncooUser" parameterType="Map" resultType="HashMap">
		SELECT i.id, i.name FROM roncoo_user i order by i.id desc limit 10
	</select>
  
</mapper>